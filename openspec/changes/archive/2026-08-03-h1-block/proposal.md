## Why
We need to build the `H1` molecule for the Hero section. This component represents the main editorial heading block (e.g., "Tierra, mundo y memoria") and heavily utilizes the `Headline` atom we just built. It forms the left side of the Hero visual structure.

## What Changes
- Create `src/components/molecules/H1.astro`.
- Implement responsive typography classes from the provided design (`.hero-h1`).
- Integrate the `Headline` atom.
- Integrate the paragraph text.

## Capabilities

### New Capabilities
- `h1-molecule`: Defines the visual constraints, layout, and sub-components for the main Hero heading.

### Modified Capabilities
- (None)

## Impact
- New component in `src/components/molecules/`.
- No impact on existing components.
