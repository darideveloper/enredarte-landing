## 1. Update Gallery Component

- [x] 1.1 Add container `id="salas-gallery"`, header class `gallery-header`, and wrapper classes (`gallery-card-large` vs `gallery-card-item`) in `Gallery.astro`.
- [x] 1.2 Add `<script>` block in `Gallery.astro` importing `gsap` and `ScrollTrigger` from `src/lib/gsap.ts` to execute header fade, card entrance cascade, and desktop-only scroll-scrubbed parallax on `gallery-card-large`.

## 2. Verification

- [x] 2.1 Run `npx astro build` to verify type safety and static build completion.
