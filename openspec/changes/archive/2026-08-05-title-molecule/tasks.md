## 1. Molecule Implementation

- [x] 1.1 Create `Title.astro` file in `src/components/molecules/`.
- [x] 1.2 Define `Props` interface with optional `linkText` and `linkHref` strings.
- [x] 1.3 Implement the `<h2>` wrapper and `<slot />` for the title content.
- [x] 1.4 Add conditional rendering for the `<a>` tag when both `linkText` and `linkHref` are provided.
- [x] 1.5 Apply flexbox utility classes to the root element (`flex justify-between items-baseline`) and styling for the `<h2>` and `<a>` elements.
