## 1. Setup Component

- [x] 1.1 Create `src/components/atoms/Headline.astro` file.
- [x] 1.2 Define the component props (`color?: "red" | "default" | "muted"`).

## 2. Implementation

- [x] 2.1 Implement the variant styles object for colors mapping to Tailwind classes (`text-crimson`, `text-ink`, `text-muted`).
- [x] 2.2 Render a `<span>` element with the base typography classes (`uppercase tracking-[0.22em] text-[10px]`) and the applied color variant.
- [x] 2.3 Include `<slot />` inside the span to render the content.
