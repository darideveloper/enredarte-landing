## 1. Implement Molecule

- [x] 1.1 Create `src/components/molecules/ImageBanner.astro` with required props (src, alt, title, href, artist, price)
- [x] 1.2 Import `Image` atom and `CardSummary` molecule
- [x] 1.3 Compose the components, placing `CardSummary` over the `Image` wrapper

## 2. Showcase Component

- [x] 2.1 Update `src/pages/design-system.astro` to add an `ImageBanner` section
- [x] 2.2 Add examples of `ImageBanner` with full data and partial data
