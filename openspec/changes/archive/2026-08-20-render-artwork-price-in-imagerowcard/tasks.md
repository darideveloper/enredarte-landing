## 1. Implementation

- [x] 1.1 Add `price={artwork.price}` to the `CardSummary` props in `src/components/molecules/ImageRowCard.astro` (line 42).
- [x] 1.2 Verify the change builds and renders correctly (`pnpm run dev` or `pnpm run build`).
