## 1. Add Target Class Identifiers & Script

- [x] 1.1 Update `Home.astro`, `Filters.astro`, `FilterBtn.astro`, and `Artworks.astro` to add target animation classes (`artworks-header`, `filter-pill`, `artwork-card-item`).
- [x] 1.2 Add `<script>` module in `Artworks.astro` (or `Home.astro`) importing `gsap` and `ScrollTrigger` from `src/lib/gsap.ts` to execute the ScrollTrigger reveal wave and interactive filter tab refresh animation.

## 2. Verification

- [x] 2.1 Run `npx astro build` to verify type safety and static build completion.
