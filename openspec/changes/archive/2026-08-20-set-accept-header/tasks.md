## 1. Client Update

- [x] 1.1 Add `Accept: application/json` header in `apiFetch()` alongside the existing `Authorization` header in `src/lib/api/client.ts`

## 2. Verify

- [x] 2.1 Confirm the build still succeeds with `pnpm run build`
- [x] 2.2 Confirm all API data loads correctly on the built site