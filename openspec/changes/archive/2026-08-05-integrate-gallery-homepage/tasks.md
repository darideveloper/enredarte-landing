## 1. Homepage Integration

- [x] 1.1 Import the `Gallery` component inside `src/components/pages/landing/Home.astro`.
- [x] 1.2 Define the `salasData` array containing the 5 mock items matching the design, inside the frontmatter.
- [x] 1.3 Render the `<Gallery salas={salasData} />` below the existing `<Hero />` component.
