## 1. Update Btn Atom Styling and Transitions

- [x] 1.1 Update `primary` variant in `Btn.astro` to set `border-crimson` pre-hover and convert to red ghost button on hover (`hover:bg-transparent hover:text-crimson hover:border-crimson`).
- [x] 1.2 Update `baseStyles` in `Btn.astro` to include `ease-in-out` timing (`transition-all duration-300 ease-in-out`).

## 2. Update FilterBtn Atom Transitions

- [x] 2.1 Update transition classes in `FilterBtn.astro` to `transition-all duration-300 ease-in-out`.

## 3. Verification

- [x] 3.1 Verify button components build cleanly and render correctly in `/design-system`.
