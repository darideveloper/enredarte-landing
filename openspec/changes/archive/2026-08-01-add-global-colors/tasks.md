## 1. Global CSS Configuration

- [x] 1.1 Add color tokens (`paper`, `ink`, `crimson`, `muted`, `border-theme`) to `@theme inline` in `src/styles/global.css`.

## 2. Refactor Components and Pages

- [x] 2.1 Refactor `src/components/atoms/Btn.tsx` to use theme tokens (`bg-crimson`, `text-paper`, `border-ink`, etc.).
- [x] 2.2 Refactor `src/pages/design-system.astro` to replace hardcoded hex values with theme token utility classes.
- [x] 2.3 Verify page rendering and visual consistency.
