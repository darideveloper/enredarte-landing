## 1. Add Animation Target Classes to Hero

- [x] 1.1 Add target class identifiers (`hero-banner`, `hero-badge`, `hero-title`, `hero-desc`, `hero-btn`, `hero-meta`) to DOM elements inside `Hero.astro`.

## 2. Add GSAP Client Script to Hero

- [x] 2.1 Add `<script>` block in `Hero.astro` importing `gsap` from `src/lib/gsap.ts` and constructing the staggered entrance timeline (`scale`, `y` translation, and `opacity` fade-in).

## 3. Verification

- [x] 3.1 Run `npx astro build` to verify type safety and static build completion.
