## 1. Update CardSummary Hover Styling

- [x] 1.1 Update `CardSummary.astro` wrapper classes to include `hover:scale-[1.03]`, `hover:-translate-y-1`, `hover:shadow-2xl`, and `transition-all duration-300 ease-out`.

## 2. Verification

- [x] 2.1 Verify build completes with `npx astro build` and test hover interaction in `/design-system` showcase.
